// Update this page (the content is just a fallback if you fail to update the page)

import IPDRDashboard from "./IPDRDashboard";

const Index = () => {
  return <IPDRDashboard />;
};

export default Index;
